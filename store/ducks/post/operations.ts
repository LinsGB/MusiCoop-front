// import postApi from '../../../api/post';
// import * as postActions from './actions';

// export const getPost = () => async (dispatch: any) => {
//   try {
//     const post: any = await postApi.getPost();

//     dispatch(postActions.SetPostAction(post));

//     return post;
//   } catch (error: any) {}
// };
